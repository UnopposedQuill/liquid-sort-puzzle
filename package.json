{
  "name": "liquid-sort-puzzle",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:server": "pnpm --filter server dev",
    "build:frontend": "pnpm --filter frontend build",
    "cap:sync": "pnpm --filter frontend cap:sync",
    "cap:run:android": "pnpm --filter frontend cap:run:android"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
